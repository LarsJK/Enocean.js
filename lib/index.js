'use strict';

var parser = require('./enocean-parser')(),
	crc = require('./crc');

var emitter = {
	emit: function (type, data, optional) {
		console.log('packet: ', type, data, optional);
	}
};

var buf = new Buffer([
	0x46, 0x55, 0x55, 0x00, 0x01, 0x01, 0x05, 0x65, 0x02, 0x07, 0x3F,
	0x55, 0x55, 0x00, 0x01, 0x01, 0x05, 0x65, 0x02, 0x07, 0x3F
]);

console.log(crc(new Buffer([0x00, 0x01, 0x07, 0x01])));

parser(emitter, buf);

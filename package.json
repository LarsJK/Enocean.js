{
  "name": "enocean.js",
  "version": "0.0.3",
  "description": "Library for communicating with enocean protocol",
  "main": "./lib/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "node lib/index",
    "test": "mocha --reporter spec",
    "coveralls": "istanbul cover _mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/iUtvikler/Enocean.js.git"
  },
  "keywords": [
    "enocean"
  ],
  "author": "Lars-Jørgen Kristiansen",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^1.17.1",
    "gulp-jshint": "^1.5.0",
    "jshint-stylish": "^0.1.5",
    "gulp-mocha": "^0.4.1",
    "gulp": "^3.5.5",
    "gulp-util": "^2.2.14",
    "mocha-lcov-reporter": "0.0.1",
    "istanbul": "^0.2.6",
    "jshint": "^2.4.4",
    "coveralls": "^2.8.0"
  },
  "dependencies": {
    "buffertools": "^2.0.1",
    "crc": "^2.0.0"
  }
}
